#include <sstream>
int GetCurrentMission(int x)
{
	int mission = 0; 

	return mission;
}

std::string GetTextMission(int CurrentMission) {
	std::string MissionText = "Kill them all";

	return MissionText;
}
